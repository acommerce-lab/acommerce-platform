<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <PackageId>ACommerce.Kits.Versions.Operations</PackageId>
    <Version>1.0.0</Version>
    <Description>
      Versions Kit — Operations layer. Defines the AppVersion contract,
      version states (Latest / Active / NearSunset / Deprecated / Unsupported),
      operation tags, the IAppVersionGate provider port (server-side decision),
      and the VersionGateInterceptor (Pre interceptor applied to every
      operation except version.* ops themselves).

      The kit follows the same shape as Auth and Subscriptions: this layer
      defines the *contract* and the *cross-cutting interceptor*; the Backend
      layer adds HTTP controllers; an app supplies the IAppVersionGate /
      IVersionStore implementations.
    </Description>
  </PropertyGroup>
  <ItemGroup>
    <!-- Pure contracts: AppVersion + IAppVersionGate + VersionTagKeys.
         Interceptor + DI extensions live in Versions.Backend; FrameworkReference
         is unused here and would propagate into WASM (NETSDK1082). -->
    <ProjectReference Include="..\..\Core\OperationEngine\ACommerce.OperationEngine\ACommerce.OperationEngine.csproj" />
    <ProjectReference Include="..\..\Core\OperationEngine\ACommerce.OperationEngine.Interceptors\ACommerce.OperationEngine.Interceptors.csproj" />
  </ItemGroup>
</Project>
