@page "/account"
@inject AppStore Store
@inject ApiReader Reader
@inject ClientOpEngine Engine
@inject NavigationManager Nav
@inject L L

<PageTitle>@(L["account.title"]) - @(L["app.name"])</PageTitle>

<AcAuthGuard IsAuthenticated="@Store.Auth.IsAuthenticated" IsArabic="@Store.Ui.IsArabic" LoginHref="/login">
    @if (Loading)
    {
        <AcLoadingState Message="@(L["common.loading"])" />
    }
    else
    {
        <div class="acs-page" style="padding: 16px">
            <AcPageHeader Title="@(L["account.title"])"
                          Subtitle="@(L["account.subtitle"])" />

            <AcCard Class="ac-mb-lg">
                <Header>
                    <strong>@(L["account.personal_info"])</strong>
                </Header>
                <ChildContent>
                    <AcField Label="@(L["account.full_name"])" Class="ac-mb-md">
                        <AcInput Value="@FullName"
                                 Placeholder="@(L["account.full_name_placeholder"])"
                                 ValueChanged="@(v => FullName = v ?? "")" />
                    </AcField>

                    <AcField Label="@(L["account.email"])">
                        <AcInput Value="@Email"
                                 Type="email"
                                 Placeholder="@(L["account.email_placeholder"])"
                                 ValueChanged="@(v => Email = v ?? "")" />
                    </AcField>
                </ChildContent>
            </AcCard>

            <AcCard Class="ac-mb-lg">
                <Header>
                    <div style="display:flex;align-items:center;gap:0.5rem">
                        <AcIcon Name="car" Size="16" />
                        <strong>@(L["account.car_details"])</strong>
                    </div>
                </Header>
                <ChildContent>
                    <p class="ac-text-muted ac-text-sm ac-mb-md">
                        @(L["account.car_details_hint"])
                    </p>

                    <AcField Label="@(L["checkout.car_model"])" Class="ac-mb-md">
                        <AcInput Value="@CarModel"
                                 Placeholder="@(L["account.car_model_placeholder"])"
                                 ValueChanged="@(v => CarModel = v ?? "")" />
                    </AcField>

                    <AcField Label="@(L["checkout.car_color"])" Class="ac-mb-md">
                        <AcInput Value="@CarColor"
                                 Placeholder="@(L["account.car_color_placeholder"])"
                                 ValueChanged="@(v => CarColor = v ?? "")" />
                    </AcField>

                    <AcField Label="@(L["checkout.car_plate"])">
                        <AcInput Value="@CarPlate"
                                 Placeholder="@(L["account.car_plate_placeholder"])"
                                 ValueChanged="@(v => CarPlate = v ?? "")" />
                    </AcField>
                </ChildContent>
            </AcCard>

            @if (!string.IsNullOrEmpty(Message))
            {
                <AcAlert Variant="@MessageVariant" Message="@Message" Class="ac-mb-md" />
            }

            <AcButton Variant="primary"
                      Block="true"
                      Text="@(Saving ? L["account.saving"] : L["account.save_changes"])"
                      Loading="@Saving"
                      Disabled="@Saving"
                      OnClick="Save" />
        </div>
    }
</AcAuthGuard>

@code {
    private bool Loading = true;
    private bool Saving;
    private string? Message;
    private string MessageVariant = "info";

    private string FullName = "";
    private string Email = "";
    private string CarModel = "";
    private string CarColor = "";
    private string CarPlate = "";

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (!firstRender) return;

        if (!Store.Auth.IsAuthenticated || !Store.Auth.UserId.HasValue)
        {
            Loading = false;
            StateHasChanged();
            return;
        }

        try
        {
            var env = await Reader.GetAsync<UserProfile>($"/api/users/{Store.Auth.UserId}");
            if (env.Data != null)
            {
                FullName = env.Data.FullName ?? "";
                Email = env.Data.Email ?? "";
                CarModel = env.Data.CarModel ?? "";
                CarColor = env.Data.CarColor ?? "";
                CarPlate = env.Data.CarPlate ?? "";
            }
        }
        catch { }

        Loading = false;
        StateHasChanged();
    }

    private async Task Save()
    {
        Message = null;
        if (string.IsNullOrWhiteSpace(FullName))
        {
            Message = L["account.name_required"];
            MessageVariant = "warning";
            return;
        }

        Saving = true;
        try
        {
            var payload = new
            {
                fullName = FullName.Trim(),
                email = Email.Trim(),
                carModel = CarModel.Trim(),
                carColor = CarColor.Trim(),
                carPlate = CarPlate.Trim()
            };

            var result = await Engine.ExecuteAsync<object>(
                ClientOps.UpdateProfile(Store.Auth.UserId!.Value), payload);

            if (result.Operation.Status == "Success")
            {
                Message = L["account.saved_successfully"];
                MessageVariant = "success";
            }
            else
            {
                Message = result.Operation.ErrorMessage
                          ?? result.Error?.Message
                          ?? L["account.save_failed"];
                MessageVariant = "danger";
            }
        }
        catch (Exception ex)
        {
            Message = ex.Message;
            MessageVariant = "danger";
        }
        finally
        {
            Saving = false;
            StateHasChanged();
        }
    }

    public class UserProfile
    {
        public Guid Id { get; set; }
        public string? FullName { get; set; }
        public string? Email { get; set; }
        public string? Theme { get; set; }
        public string? Language { get; set; }
        public string? CarModel { get; set; }
        public string? CarColor { get; set; }
        public string? CarPlate { get; set; }
    }
}
