{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "JWT": {
    "Issuer":   "https://admin.ejar.sa",
    "Audience": "ejar-admin-api"
  },

  "Cors": {
    "AllowedOrigins": [
      "https://admin.ejar.sa"
    ]
  },

  "_comment_cache_realtime": "REMINDER: set Cache:Redis:ConnectionString and Realtime:Redis:ConnectionString in production. Use the SAME Redis instance as Ejar Customer/Admin so chat state spans all hubs (provider <-> renter conversation routing depends on it).",
  "Cache":    { "Redis": { "ConnectionString": null } },
  "Realtime": { "Redis": { "ConnectionString": null } }
}
