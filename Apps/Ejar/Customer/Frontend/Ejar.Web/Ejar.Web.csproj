<Project Sdk="Microsoft.NET.Sdk.Web">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RollForward>LatestMajor</RollForward>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Ejar.Web</RootNamespace>
    <AssemblyName>Ejar.Web</AssemblyName>
    <UserSecretsId>ejar-web</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <!-- المكتبة المشتركة لعميل إيجار — تحتوي كل الصفحات والخدمات.
         المضيف هنا (Server) يضيف فقط: AddRazorComponents.AddInteractiveServerComponents
         + HttpClient مسمّى "ejar" بـ BaseAddress من appsettings. -->
    <ProjectReference Include="..\..\Shared\Ejar.Customer.UI\Ejar.Customer.UI.csproj" />
  </ItemGroup>
</Project>
