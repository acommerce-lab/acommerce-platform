{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Database": {
    "Provider": "mssql",
    "ConnectionString": "Server=db48897.public.databaseasp.net;Database=db48897;User Id=db48897;Password=r=2P9S%yM_e8;Encrypt=True;TrustServerCertificate=True;MultipleActiveResultSets=True;"
  },

  "JWT": {
    "Issuer": "https://ejar.ye",
    "Audience": "ejar-api",
    "AccessTokenLifetime": "30.00:00:00"
  },

  "Cors": {
    "_comment": "AllowedOrigins = origins صريحة. AllowedOriginPatterns = wildcards (للاستضافات المشتركة كـ runasp.net التي تختار subdomain تلقائياً). Override الإنتاج عبر env var: Cors__AllowedOrigins__0=https://customer.example.com",
    "AllowedOrigins": [
      "https://ejar.ye",
      "https://www.ejar.ye"
    ],
    "AllowedOriginPatterns": [
      "https://*.runasp.net"
    ]
  },

  "_comment_cache_realtime": "REMINDER: set Cache:Redis:ConnectionString to enable Redis-backed ICache + RedisConnectionTracker (multi-instance safe). Realtime:Redis:ConnectionString turns on the SignalR cluster backplane (cross-host SendToGroup/User). Same Redis instance is fine for both — it's a separate ConnectionMultiplexer per concern, but they pool internally.",
  "Cache":    { "Redis": { "ConnectionString": null } },
  "Realtime": { "Redis": { "ConnectionString": null } }
}
